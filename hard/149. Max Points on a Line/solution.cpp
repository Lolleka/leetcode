// https://leetcode.com/problems/max-points-on-a-line
// hard
// $array, #hash-table, #math, #geometry

class Solution {
public:
    int maxPoints(vector<vector<int>>& points) {
        // count how many pairs have the same slope using a hash table
        // given the constraints, this problem can be brute forced with two nested
        // for loops
    }
};

int main (int argc, char *argv[]) {
    
    return 0;
}
